#include <stdio.h>
#include <stdlib.h>

int main() {
    int total, lado1, lado2;
    printf ("digite lado 1:");
    scanf ("%d", &lado1);
    printf ("digite lado 2:");
    scanf ("%d", &lado2);
    total = lado1*lado2;
    printf ("a area do quadrado e: %d", total);
	retun 0;
}
